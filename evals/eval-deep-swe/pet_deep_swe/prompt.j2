{{ instruction }}

---
Execution notes from the eval harness (pre-authorized):
- You are running unattended inside an isolated container. Nobody will reply to
  questions — decide and act on your own, using your tools, until the task is done.
- The repository is at your current working directory. Your file tools require
  absolute paths.
- When you are done you MUST create a new branch off main and `git commit` ALL of
  your changes. Committing is required and explicitly authorized here; uncommitted
  work is discarded and scores zero.
