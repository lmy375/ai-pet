> 该文件是产品需求池，描述待开发的所有产品需求。AI 可通过自己对产品需求的分析、代码的分析提出新的产品需求（包括功能的实现、Bug 的修复、代码的重构等），但只有经过用户确认的需求，才会进入开发流水线。开发完成后从该文件中移除。
> 1. 如果没有已经确认的需求，则自主开始需求分析，代码分析，提出新的需求，每个需求就不超过 100 个字描述。每个需求一行，添加到 `待确认` 目录下。
> 2. 每个需求要是一个具体、有价值的、工作量适中的任务。任务要为了实现 `GOAL.md` 中的目标而制定。任务不能过度复杂，如果某个任务过于宏大，可以考虑放到 `GOAL.md` 中。
> 3. 用户会手动将 `待确认` 中的项目移动到`已确认`中。你不可以直接向 `已确认` 中写入内容。
> 4. `已确认` 中的每一条需求，在实现时，在 docs 中创建一个 `index-yyyymmdd-hhmm-title.md` 的文件。编写开发计划，记录开发结果。
> 5. 开发完成后，将上面的文件移动到 `done` 中。保持本文件处于一个简洁的状态。如果这项任务完成了一个值得用户关心的产品亮点，将其更新到根目录下的 `README.md` 中

# 已确认


# 待确认