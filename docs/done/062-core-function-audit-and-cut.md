# 062 · 核心功能审计 + 大幅 cut

60+ done req 累计后 UI 雪崩、TG API 触顶（061）、用户感觉"过于复杂"。本 req 不加功能，定义核心 + 列具体删除清单。

**核心功能（保留）**：
- pet figure（含 047 mood badge / 054 idle motion）+ ChatMini 对话
- 一个 user-driven memory store（手动 add + 简单 search 056）
- reminder（基础时间提醒 + 030 snooze）
- 一种 proactive 触发：morning_briefing（016）+ welcome_back（008）合并简化
- 多模态输入：001 TG photo + 031 file + 002 screenshot（输出 003 morning 图保留）
- PanelChat / PanelMemory / PanelTasks / PanelSettings / PanelPersona / PanelDebug 六个 panel；其余 panel 删

**删除清单（done req，按子系统）**：
- proactive 触发线收敛：删 039 evening_briefing / 032 anniversary_extract / 034 surprise_gift / 036 inter_day_mood / 042 reminder_escalate / 007 memory_follow_up / 018 propose_from_memory（保留 008/016/reminder 三条）
- memory 自组织收敛：删 023 session_distill / 024 forget_propose / 025 consolidate_propose / 027 topic_arc / 029 self_note / 040 lexicon_mirror / 043 people_graph / 044 places_graph（保留 user 手动 add + 056 search）
- task 子系统收敛：删 010 subtask_propose / 011 scheduled_report / 012 deferred_task / 013 return_briefing / 014 PanelReports / 015 conversational_cancel / 020 task_chain / 021 mood_weekly_builtin / 037 goal_tracking / 041 calendar_conflict（reminder 仅保留 030 snooze + 022 time_ambiguity_confirm）
- 自我进化细分收敛：删 006 intent_profile / 019 persona_style_self_tune / 026 user_stress_detect / 017 mood_task_interplay / 035 user_routine_learn（mood 仅保留 current_mood + 历史 log，不做 fancy 衍生）
- panel 收敛：删 PanelReports（014）+ PanelMood（028）；其内信息合并到 PanelMemory / PanelPersona 单 section
- audit / observability chip：commit history 30+ 个 chip 累积全删（PanelMemory 058 已部分覆盖；扩展到 PanelTasks）
- TG 命令：按 061 家族 namespace 收敛 ≤ 20 条注册命令；删 cat_growth_* / cat_decay_* / streak_* / pin_grow / pinned_drop / find_speech_* / recent_renames / aliases / here_* 多变种等独立命令

**删除原则**：
- 后端代码（rs / store / proactive 子模块）一并删，不只是 UI 隐藏
- 删后的数据文件保留 30d 备份（mood_history.log / butler_history 不删）以防 user 反悔
- 删之前列出依赖图，确认无其他保留 req 引用；有引用的回头改保留 req 后再删

**实施顺序**（避免一次性破坏过大）：
1. PanelReports + PanelMood + audit chip → 安全先删
2. proactive 触发线收敛 → 删 6 条触发后观察一周
3. memory 自组织 → 同上
4. TG 命令家族收敛（061）
5. task 子系统 → 最后，影响最大

---
进度（本刀实施 step1a：PanelReports 删）：
- 删 `src-tauri/src/panel_reports.rs`（430 行）
- 删 `src/components/panel/PanelReports.tsx`（327 行）
- `lib.rs` 去掉 `mod panel_reports` + 2 个 Tauri 命令注册（`panel_reports_list` / `panel_reports_get`）
- `PanelApp.tsx` 去掉 import + 「回查」tab 从 TABS / 渲染分支
- 删 TG 命令 `/reports_list` / `/report <id>`：TgCommand 变体 / name() / title() / 默认空 group / parser / bot.rs handler 一并清
- `cargo build` ✅ clean。剩 8 处 PanelReports 字面量在内部 doc comment（proactive.rs / scheduled_report.rs / deferred_tasks.rs / surprise.rs / cancel_task_tool.rs / commands.rs / defer_task_tool.rs）—— 非 load-bearing，遗留 ref 留下一刀清理（其中 `defer_task_tool.rs` 是 LLM 看到的 tool description 含 "PanelReports view filters..."，下一刀优先清此条避免 LLM 看到不存在 UI 名）
- **后续步骤**（按 spec 顺序）：
  1. ✅ step1a：PanelReports 删
  2. ✅ step1b：PanelMood 删（本刀 / 删 PanelMood.tsx 537 行 + mood_chart.rs 420 行 + Tauri 命令 get_mood_chart_data + PanelApp.tsx 「心情」tab + TG `/mood_chart` + TgCommand::MoodChart 变体 / parser / handler；mood.rs / mood_history.log 保留——spec「mood 仅保留 current_mood + 历史 log」对应；leftover refs 0）
  3. step1c：audit chip 删（spec 「commit history 30+ 个 chip 累积全删」，需扫所有 PanelMemory chip + 058 已部分覆盖）—— **frontend only，跳过留 user 接力**
  4. step2：proactive 触发线 7 条（039/032/034/036/042/007/018）—— 进行中
    - ✅ step2a: 036 inter_day_mood 删（删 proactive/inter_day_mood.rs + maybe_run_morning_briefing 注入块；mood/dominant_policy 逻辑随之消失）
    - ✅ step2b: 039 evening_briefing 删（删 proactive/evening_briefing.rs + maybe_run_evening_briefing 函数 192 行 + LAST_EVENING_BRIEFING_DATE static + run_proactive_turn 抑制 check + maybe_run_surprise_gift Gate 5 抑制 check + tick loop 调用；leftover refs 0）
    - ✅ step2c: 034 surprise_gift 删（删 surprise.rs 整模块 + maybe_run_surprise_gift 函数 170 行 + LAST_SURPRISE_GIFT_TIME static + tick loop 调用 + lib.rs mod / surprise_log_list Tauri 命令 + self_note TriggerKind::SurpriseEmit 变体 + TG /surprise_log（TgCommand 变体 / name / title / parser / bot.rs handler）；ProactiveNotificationKind::Surprise enum 变体留作 052 协议占位（`#[allow(dead_code)]`，与 enum 整体保持一致）；doc 注释残留 6 处但全为字符串而非代码 ref，留下一刀清理）
    - ✅ step2d: 032 anniversaries 删（删 anniversaries.rs 整模块 + lib.rs mod / 3 Tauri 命令（anniversary_list/add/delete）+ proactive.rs::maybe_run_morning_briefing 内 anniversary enrich block + TG `/anniversaries` `/anniversary_add` `/anniversary_del` 三件套（TgCommand 变体 / name / title / parser / bot.rs 3 个 handler）+ memory_retrieval.rs::RetrieveSource::Anniversaries 变体（含 label / parse / ALL_SOURCES / 扫描分支 / label_protocol_stable 测试断言 / doc）+ retrieve_memory_tool 工具描述里的 anniversaries 行；leftover refs 2 处全是 doc 字符串）
    - ✅ step2e: 042 reminder_escalate 删（删 proactive/reminder_escalate.rs 整模块 + proactive.rs::maybe_run_reminder_escalate 函数 160 行 + tick loop 调用 + build_reminders_hint 中 record_initial_fire hook + cancel_task_tool 中 reminder ack clear_for_title + snooze_reminder_tool 中 clear_for_title + telegram::bot::handle_text_message 中 clear_all_pending + commands::chat::chat 中 clear_all_pending + ESCALATION_STATE static / REMINDER_ESCALATE_FAIL_COUNT counter / EscalationState / EscalateAction enum 全部随模块删除；leftover refs 1 处 doc 字符串）
    - ✅ step2f: 007 memory_follow_up 删（删 proactive/memory_follow_up.rs 整模块 + proactive.rs::maybe_run_memory_follow_up 函数 180 行 + LAST_MEMORY_FOLLOW_UP_TIME static + tick loop 调用 + `mod memory_follow_up` 声明；`build_recent_session_corpus` 保留——topic_arc / lexicon_learn / forget_propose 仍依赖；leftover refs 5 处全为 doc 字符串）
    - ✅ step2g: 018 propose_from_memory 删（仅一处 `[from-memory:]` 协议引用在 defer_task_tool description 里——清理；018 全程靠 010 propose_task tool 落 deferred task 时**LLM 自约束 marker**，无独立 backend store / function，描述清理即等于 018 删干净）
  5. step3：memory 自组织 8 条（023/024/025/027/029/040/043/044）
    - ✅ step3a: 023 session_distill 删（删 session_distill.rs 整模块 + lib.rs mod + proactive.rs::maybe_run_session_distill 函数 150 行 + LAST_DISTILL / LAST_DISTILL_TIME statics + tick loop 调用；memory_retrieval.rs RetrieveSource::Distill 变体一并删除（含 label/parse/ALL_SOURCES/scan 分支 + 双计跳 distill 逻辑 + 测试断言 + 工具描述）；legacy `[session_distill:` marker entries 在 memory store 仍 findable via Memory source；leftover refs 4 处全为 doc 字符串）
    - ✅ step3b: 024 forget_propose 删（删 proactive/forget_propose.rs 整模块 + proactive.rs::maybe_run_forget_propose 函数 175 行 + LAST_FORGET_PROPOSE_WEEK static + 3 consts + tick loop 调用 + mod 声明；leftover refs 1 处 doc 字符串）
    - ✅ step3c: 025 consolidate_propose 删（同步推进，删 proactive/consolidate_propose.rs 整模块 + maybe_run_consolidate_propose 函数 160 行 + LAST_CONSOLIDATE_PROPOSE_WEEK static + 3 consts + tick loop 调用 + mod 声明；leftover refs 0）
    - ✅ step3d: 027 topic_arc 删（删 src-tauri/src/topic_arc.rs 整模块 + tools/set_topic_arc_tool.rs LLM tool + lib.rs mod / Tauri 命令 topic_arc_list + tools/mod.rs / registry.rs SetTopicArcTool 注册 + proactive.rs::maybe_run_topic_arc_scan 函数 125 行 + LAST_TOPIC_ARC_WEEK static + 3 consts + tick loop 调用 + 7 个 inject_topic_arc_layer 站点（5 个统一 messages 通过 replace_all + telegram::bot + commands::chat::chat + run_proactive_turn 的 chat_messages variant + memory_follow_up 已先前删除）+ TG `/topics` `/topic_clear` `/topic_pin` 三件套（TgCommand 变体 / name / title / parser / bot.rs 3 handlers）；leftover refs 5 处全为 doc 字符串）
    - ✅ step3e: 029 self_note 删（删 src-tauri/src/self_note.rs 整模块 + lib.rs mod / 3 Tauri 命令（self_note_list / self_note_set_paused / self_note_is_paused）+ 3 个 spawn_record hook 站点（proactive::maybe_run_daily_review 末尾 / deferred fire 成功后 / communication_prefs::add_preference）+ TG `/self_notes` `/self_notes_pause` `/self_notes_resume` 三件套（变体 / name / title / parser / bot.rs handlers）+ memory.rs MemoryIndex::default 中 "self_note" cat 初始化 + memory_retrieval.rs RetrieveSource::SelfNote 变体（label / parse / ALL_SOURCES / scan 分支双重过滤 / 测试断言 / 工具描述）+ Memory 源原 self_note skip 逻辑（连同 SelfNote 一起清；legacy self_note 入口的 memory items 现在被 Memory 源平等扫描——pet-owned 隔离失效但 029 整线已删，无新条目）；leftover refs 0 处）
    - ✅ step3f: 040 user_lexicon 删（删 src-tauri/src/user_lexicon.rs 整模块 + tools/set_user_lexicon_tool.rs LLM tool + lib.rs mod / 3 Tauri 命令（user_lexicon_list/_clear/_pin）+ tools/mod.rs / registry.rs SetUserLexiconTool 注册 + proactive.rs::maybe_run_lexicon_learn 函数 105 行 + LAST_LEXICON_LEARN_WEEK static + 4 个 const + tick loop 调用 + 7 个 inject_user_lexicon_layer 站点（5 个统一 messages 通过 replace_all + run_proactive_turn 的 chat_messages variant + telegram::bot::run_chat_turn + commands::chat::chat）+ TG `/lexicon` `/lexicon_clear` `/lexicon_pin` 三件套（TgCommand 变体 / name / title / parser / bot.rs handlers）+ build_recent_session_corpus 已是孤儿函数随之删除（最后调用方 lexicon_learn 删后无 caller）+ person_edit_tool description 里的 "user_lexicon (language)" ref 清理；leftover refs 0 处）
    - ✅ step3g: 043 user_people 删（删 src-tauri/src/user_people.rs 整模块 612 行 + tools/person_edit_tool.rs LLM tool 121 行 + lib.rs mod / 4 Tauri 命令（user_people_list/_clear/_pin/_merge）+ tools/mod.rs / registry.rs PersonEditTool 注册 + 5 个 inject_user_people_layer 站点（4 个统一 messages 通过 replace_all + run_proactive_turn 的 chat_messages variant + telegram::bot::run_chat_turn + commands::chat::chat）+ TG `/people` `/people_clear` `/people_pin` `/people_merge` 四件套（TgCommand 变体 / name / title / parser / bot.rs 4 handlers ~68 行）+ user_places.rs 模块 doc / place_edit_tool description 里的 "people (person_edit)" ref 清理；leftover refs 0 处。注：043 spec 列入 062 删除清单——butler 方向（feedback_pet_butler_direction）允许保留显式 add，user_people 的"自动 mention + 累计触发"模式属于 022/043 一系自我进化细分，与简化方向冲突——按 spec 删）
    - ✅ step3h: 044 user_places 删（删 src-tauri/src/user_places.rs 整模块 590 行 + tools/place_edit_tool.rs LLM tool 119 行 + lib.rs mod / 4 Tauri 命令（user_places_list/_clear/_pin/_merge）+ tools/mod.rs / registry.rs PlaceEditTool 注册 + 5 个 inject_user_places_layer 站点（4 个统一 messages 通过 replace_all + run_proactive_turn 的 chat_messages variant + telegram::bot::run_chat_turn + commands::chat::chat）+ TG `/places` `/place_clear` `/place_pin` `/place_merge` 四件套（TgCommand 变体 / name / title / parser / bot.rs 4 handlers ~65 行）；leftover refs 0 处。**step3 整段（memory 自组织 8 条 023/024/025/027/029/040/043/044）全部完成**——下次 cron 进 step4 TG 家族 namespace 重构或 step5 task 子系统）
  6. step4：TG 家族 namespace 重构（061 后续）
    - ✅ step4a: cat_growth / cat_decay 5 TG 命令家族删（/cat_growth_7d /cat_growth_30d /cat_growth_today /cat_decay_7d /cat_decay_30d）— commands.rs 5 TgCommand 变体 + 5 name() arm + 5 title group + 5 parser arm + 5 long help match arm + 5 fall-through help line + 2 个 audit family list（English / Chinese 各 5 条）+ ALL_KNOWN_COMMANDS 3 list（line ~2757 / 9980 / 10451）共 15 string keys 清除 + help_table cat family 重写（从「🌱 cat 活跃度」+ 7 命令 → 「📚 cat（memory category）」+ 2 命令 /cat_top /here_top_cat）+ cat_top 长 help 内 cross-reference 清理 + audit_summary 长 help 内 cat 7d 行清理 + format_audit_summary_reply 签名瘦身（去掉 cat_active_7d_count 参数 + bot.rs caller 同步去掉 compute_cat_growth_rows 调用 + 2 个测试同步签名）+ cat_top empty fallback 内 /cat_growth_7d 教学 ref 清理 + 删 5 tests block 共 ~220 行（cat_growth_today/30d/7d/decay_30d/7d parser + format tests）+ 删 format_cat_growth_reply / format_cat_growth_7d_reply / format_cat_growth_today_reply / format_cat_decay_reply / format_cat_decay_7d_reply 5 个 format functions 共 ~110 行 + bot.rs 删 compute_cat_growth_rows / compute_cat_decay_rows 2 个 helpers 共 ~80 行 + 5 个 handler arm（CatDecay7d/CatDecay30d/CatGrowth7d/CatGrowthToday/CatGrowth30d）+ help_table 测试列 family name `🌱 cat 活跃度` → `📚 cat（memory category）`；leftover refs 0 处）
    - ✅ step4b: find_speech 3 TG 命令家族删（/find_speech /find_speech_today /find_speech_yesterday）— commands.rs 3 TgCommand 变体 + 3 name() arm + 3 title group + 3 parser arm + 3 long help match arm + 3 fall-through help line + 2 个 audit family list（English / Chinese 各 3 条）+ ALL_KNOWN_COMMANDS 3 list 共 9 string keys 清除 + 3 个 format functions（format_find_speech_reply / format_find_speech_today_reply / format_find_speech_yesterday_reply）共 ~90 行 + 删 13 tests（5 个 find_speech + 4 个 today + 4 个 yesterday block）~250 行 + bot.rs 3 个 handler arm（FindSpeech / FindSpeechToday / FindSpeechYesterday）共 ~160 行 + help_table find/search family 移除 `/find_speech /find_speech_today /find_speech_yesterday`（仅留 find_in_detail 系列）+ help_table speech family 移除 `/find_speech` 条目（仅留 last_speech / show_speech / recent_chats）+ show_speech 长 help 内 find_speech cross-ref 清理 + find_in_detail 长 help 内 find_speech cross-ref 清理 + idle_7d 注释 `/find_speech 系列 8 稍宽` cross-ref 清理；leftover refs 1 处 doc 字符串）
    - ✅ step4c: here_* 7 TG 变种删（/here_until /here_status /here_recent_done /here_top_cat /here_clear /here_idle /here_pin）— 保留 base /here 单命令。commands.rs 7 TgCommand 变体 + 7 name() arm + 6 title group line（HerePin/Idle/Clear/TopCat/RecentDone/Status block）+ HereUntil 单独 title arm + 7 parser arm + 7 long help match arm（sed-bulk delete）+ 7 fall-through help line（sed delete）+ 2 个 audit family list（English / Chinese 各 7 条）+ ALL_KNOWN_COMMANDS 3 list 共 21 string keys 清除 + 7 个 format functions（format_here_until_reply / format_here_status_reply / format_here_recent_done_reply / format_here_top_cat_reply / format_here_clear_reply / format_here_idle_reply / format_here_pin_reply）共 ~200 行 + 删 22 tests（awk 范围删除 line 15273-15584，~312 行）+ bot.rs 7 个 handler arm（HereUntil/HereStatus/HereRecentDone/HereTopCat/HereClear/HereIdle/HerePin）共 ~280 行 + help_table cat family 移除 `/here_top_cat` ref + help_table pin family 移除 `/here_pin` ref + help_table idle family 移除 `/here_idle` ref + help_table digest family 移除 `/here_recent_done` ref + help_table system family 移除 `/here_status /here_until /here_clear` 3 ref + 4 family list entry block（here_pin/top_cat/idle/recent_done）+ help_table 长 help 内 cat 行 `/here_top_cat` ref 清理；leftover refs 2 处 doc 字符串（proactive.rs::here_pin 对偶注释）)
    - ✅ step4d: 6 single TG audit 命令删（/pinned_drop_7d /pin_grow_7d /aliases /streak_pin /recent_renames /done_streak_chart）— commands.rs 6 TgCommand 变体（PinnedDrop7d / PinGrow7d / Aliases / StreakPin / RecentRenames / DoneStreakChart）+ 6 name() arm + 2 title group arms（RecentRenames + Aliases title 引用）+ 6 parser arm + 6 long help match arm（sed delete）+ 6 fall-through help line（sed delete）+ 2 个 audit family list（English / Chinese 各 6 条）+ ALL_KNOWN_COMMANDS 3 list 共 18 string keys 清除 + 7 个 format functions / helpers（format_recent_renames_reply / format_done_streak_chart_reply / format_streak_pin_reply / reconstruct_alias_chain / format_aliases_reply / format_pin_grow_7d_reply / format_pinned_drop_7d_reply）+ 2 个 done_streak_chart 专用 helpers（compute_daily_done_counts / render_sparkline）共 ~450 行 + bot.rs 6 个 handler arm（RecentRenames / DoneStreakChart / StreakPin / PinGrow7d / PinnedDrop7d / Aliases）共 ~330 行 + 删 ~360 行 tests（awk 范围 delete 2 段：recent_renames+done_streak_chart 152 行 + aliases+pin_grow+pinned_drop 183 行 + streak_pin parser/format tests ~25 行；保留 compute_pin_streak 5 个 tests 因 audit_summary 仍依赖该 helper）+ audit_summary 长 help 重写（去除 /streak_pin / /recent_renames deep-dive 链接 — 数据仍计算但 link 删）+ help_table cat / pin / streak / rename 4 family list 重构 + help_table 长 help 内 cat / streak / rename 行 cross-ref 清理 + recent_pins 长 help 内 /pin_grow_7d / /pinned_drop_7d / /streak_pin cross-ref 清理 + recent_pins fall-through line `/pin_grow_7d 的 N-cap 兄弟` cross-ref 清理 + 2 个 audit_summary tests 同步 assert（去除 /streak_pin / /recent_renames substring asserts）+ help_table 测试 cmd list 替换 `/streak_pin` → `/streak`, 删 `/aliases`；leftover refs 0 处）
  7. step5：task 子系统 10 条（影响最大留最后）
    - ✅ step5a: 037 goal_tracking 删（删 src-tauri/src/goals.rs 整模块 560 行 + tools/goal_edit_tool.rs LLM tool 160 行 + lib.rs mod / 4 Tauri 命令（goal_list / goal_progress_cmd / goal_complete_cmd / goal_delete_cmd）+ tools/mod.rs / registry.rs GoalEditTool 注册 + proactive.rs::maybe_run_goal_check_in 函数 145 行 + LAST_GOAL_CHECKIN_WEEK static + 4 const（GOAL_CHECKIN_WEEKDAY / TARGET_HOUR / MINUTE / GRACE_MIN）+ tick loop 调用 + 6 个 inject_goals_layer 站点（4 proactive 通过 replace_all + run_proactive_turn 的 chat_messages + telegram::bot::run_chat_turn + commands::chat::chat）+ TG `/goals` `/goal_progress` `/goal_done` `/goal_del` 四件套（TgCommand 变体 / name / title / parser / bot.rs 4 handlers ~80 行）；leftover refs 0 处）
    - ✅ step5b: 041 calendar_conflict 删（删 src-tauri/src/calendar_conflict.rs 整模块 196 行 + lib.rs mod 声明 + telegram::bot::run_chat_turn / commands::chat::chat 各 1 个 inject_calendar_conflict_layer 站点 + 注释说明清理；leftover refs 0 处。注：041 是纯 inject-only 协议层 — 无 store / 无 Tauri 命令 / 无 TG 命令，删除范围比 037 小）
    - ✅ step5c: 015 conversational_cancel 删（删 tools/cancel_task_tool.rs LLM tool 235 行 + tools/mod.rs / registry.rs CancelTaskTool 注册 + 2 个 mark_cancelled 函数（deferred_tasks.rs 19 行 + scheduled_report.rs 18 行 — cancel_task tool 删除后变孤儿）+ 3 处 doc comment GOAL 015 ref 清理（deferred_tasks::TaskStatus::Cancelled / scheduled_report::cancelled 字段 / scheduled_report::is_due cancelled 分支注释）；leftover refs 0 处。注：015 是 LLM tool-only 协议（无 store / Tauri / TG 命令）— Cancelled 状态值保留（serde 兼容旧 JSON）但 mark API 已删；后续 011 scheduled_report / 012 deferred_task 整删时 enum 一并消失）
    - ✅ step5d: 010 subtask_propose 删（删 src-tauri/src/subtask_helpers.rs 整模块 266 行 + lib.rs mod 声明 + Tauri 命令 butler_subtask_progress 注册 + 2 个 inject_subtask_propose_layer 站点（telegram::bot + commands::chat）+ 2 处 doc comment cross-ref 清理（communication_prefs / time_ambiguity 内 `inject_subtask_propose_layer` ref 注释）；leftover refs 0 处。注：010 设计上是「LLM 自判 vague task 后邀请拆解」的 inject-only system rule — 无 backend store / 仅一个 Tauri 命令 butler_subtask_progress（progress 计数 helper）+ 一段 ~200 字 system rule。propose_task LLM tool 是另一回事不在 010 范围）
    - ✅ step5e: 013 return_briefing 删（删 proactive/welcome_back.rs 内 BriefingItemKind enum 4 变体 + BriefingItem struct + collect_briefing_items 函数 ~42 行 + format_welcome_briefing_intent 函数 ~50 行 + number_circle helper 函数 + 5 个 tests block ~100 行 + proactive.rs::maybe_run_welcome_back 内 13 行 briefing aggregation 逻辑 fallback 到 format_welcome_back_intent base + decision log briefing count 字段移除；leftover refs 0 处。注：013 是 008 welcome_back 的 briefing-augment 上层（spec：008 welcome_back 保留）— 删除 013 后 welcome_back 回退到「欢迎回来」单句问候 base；butler_history events（report_fired / deferred_fired / archive / deferred_postponed）仍在落盘但不再被聚合为 briefing 三行列表。017 mood-postpone 的 DeferredPostponed kind 也随之删除——该值仍可在 butler_history.log 老 entry 内存在但不再被消费）
    - ✅ step5f: 020 task_chain 删（删 deferred_tasks.rs::DeferredTask::depends_on 字段（+ serde default）+ add_task 函数签名去掉 depends_on 参数 + pick_oldest_pending 内 dependency filter 逻辑（4 行）+ stale_chain_roots 函数 ~16 行 + descendants_of 函数 ~14 行 + mark_stale_chains 函数 ~30 行 + CHAIN_STALE_DAYS const + 5 tests block（pick_skips_pending_with_unmet_dependency / pick_picks_child_after_root_done / pick_skips_when_dependency_failed_or_cancelled / pick_skips_when_dependency_missing / stale_chain_roots_finds_only_pending_older_than_14d / descendants_walks_chain_recursively）~95 行 + mk_dep test helper ~14 行 + tools/defer_task_tool.rs `depends_on` JSON schema field + execute_impl 内 depends_on extraction + add_task 调用 + log/return JSON depends_on key + proactive.rs maybe_run_deferred_task 内 mark_stale_chains 调用 + TG `/chain` 命令（TgCommand::Chain 变体 / name / title group / parser / bot.rs handler ~85 行）；leftover refs 0 处。注：020 是 012 deferred_task 内部的 chain dependency 子特性 — depends_on 字段删除 + chain detection / stale sweep 删除；DeferredTask base store / fire pipeline / TG /queue 等 012 主体保留待后续 step5g 整删）
    - ✅ step5g: 012 deferred_task 删（删 src-tauri/src/deferred_tasks.rs 整模块 358 行 + tools/defer_task_tool.rs 67 行 + lib.rs mod 声明 + tools/mod.rs / registry.rs DeferTaskTool 注册 + BUILTIN_TOOL_NAMES `"defer_task"` 项 + proactive.rs::maybe_run_deferred_task 整函数 ~240 行（awk 范围 3425-3666 删）+ DEFERRED_POSTPONE_LOG_DEDUP static + LAST_DEFERRED_FIRE_TIME static + tick loop 调用 + TG `/queue` `/queue_del` 双命令（TgCommand 变体 / name / title group / parser / bot.rs handlers ~35 行）+ 同时删 017 mood_task_interplay 整 submodule proactive/mood_task_interplay.rs（因 012 是唯一 caller，删除 012 后变孤儿——017 本身也在 062 spec 删除清单内）+ proactive.rs `mod mood_task_interplay` 声明；leftover refs 1 处 doc 字符串。注：012 删除影响最大——所有 LLM "等会儿弄完告诉我" 派单流程消失；后续 user 改用直接 /task 创建 + 主动 /done 收尾的简化路径；DeferredTask::Cancelled enum 变体随模块删除一并消失，scheduled_report::cancelled 字段仍独立保留）
    - ✅ step5h: 011 scheduled_report + 021 mood_weekly_builtin 整删（删 src-tauri/src/scheduled_report.rs 整模块 585 行 + tools/scheduled_report_tool.rs LLM tool 144 行 + lib.rs mod 声明 + tools/mod.rs / registry.rs ScheduleReportTool 注册 + BUILTIN_TOOL_NAMES `"schedule_report"` 项 + proactive.rs::maybe_run_scheduled_reports + run_one_scheduled_report 两个函数 ~190 行（awk 范围 3226-3413 删）+ tick loop 调用 + TG `/reports` `/report_del` `/report_restore_builtin` 三命令（TgCommand 变体 / name / title group / parser / bot.rs handlers ~70 行）；021 sub-feature 随 011 整模块自动消失；leftover refs 4 处 doc 字符串）
  8. step6：自我进化细分收敛 5 条（spec 漏列 step 索引；本次发现 062 整删时遗漏；005/019/026/017/035 全在 spec 删除清单）
    - ✅ step6a: 035 user_routine_learn 删（删 src-tauri/src/user_routine.rs 整模块 451 行 + lib.rs mod 声明 / 2 Tauri 命令（routine_get / routine_set）+ proactive.rs::maybe_run_routine_learn 函数 30 行 + LAST_ROUTINE_LEARN_WEEK static + 4 const（ROUTINE_LEARN_WEEKDAY / TARGET_HOUR / MINUTE / GRACE_MIN）+ tick loop 调用 + TG `/routine` `/routine_set` 双命令（TgCommand 变体 / name / title / parser / bot.rs handlers ~50 行）；leftover refs 0 处。注：035 是 「Mon 04:00 周扫 butler_history 推 user 起床 / 睡前 EWMA 中位数」纯数据 aggregate；删除后 user 作息推断功能消失，但 reminder 系统仍可独立工作。017 mood_task_interplay 已在 step5g 顺手删除）
    - ✅ step6b: 026 user_stress_detect 删（删 src-tauri/src/user_stress.rs 整模块 346 行 + lib.rs mod 声明 + 4 个 inject_stress_layer 站点（commands/chat::chat + telegram/bot::run_chat_turn + proactive run_proactive_turn / morning_briefing / scheduled_reports 全部已 step5h 删除剩 3 个）+ 2 个 spawn_record_from_content 站点（chat + bot）+ proactive::maybe_run_morning_briefing 内 is_in_low_distraction_mode 判断 + short-mode override 注入 ~13 行 + 1 个 mut → 非 mut 修正；leftover refs 0 处。注：026 是「最近 user 消息含压力关键词 → low-distraction 模式 → 早安切短 + 共情口吻」启发式分类器；删除后所有路径走 normal mode）
    - ✅ step6c: 006 intent_skill_profile 删（删 src-tauri/src/skill_profile.rs 整模块 402 行 + lib.rs mod 声明 / 1 Tauri 命令 get_top_intents_30d + 2 个 spawn_record_from_content 站点（commands/chat::chat + telegram/bot::run_chat_turn）+ TG /whoami 内 tally_recent_30d + intent histogram 注入 ~10 行 切换回 format_whoami_reply 基本版 + format_whoami_reply_with_intents wrapper 函数 ~35 行 删除（base format_whoami_reply 保留）；leftover refs 0 处。注：006 是「fire-and-forget 把 user 消息分类为 intent → 30d tally 喂进 /whoami」；删除后 /whoami 不再显「最近 30d 你最常找我」一行；intent_history.log 落盘已删但 log 文件本身遗留磁盘等用户手动清）
    - ✅ step6d: 019 persona_style_self_tune 删（删 src-tauri/src/communication_prefs.rs 整模块 302 行 + tools/communication_pref_tool.rs LLM tool 155 行 + lib.rs mod 声明 / 1 Tauri 命令 communication_prefs_list + tools/mod.rs / registry.rs SetCommunicationPreferenceTool 注册 + BUILTIN_TOOL_NAMES `"set_communication_preference"` 项 + 5 个 inject_communication_prefs_layer 站点（commands/chat::chat + telegram/bot::run_chat_turn + proactive run_proactive_turn / morning_briefing / welcome_back / scheduled_reports 此前已删除剩 3 个）+ TG `/persona` `/persona_clear` `/persona_reset` 三命令（TgCommand 变体 / name / title / parser / bot.rs handlers ~50 行）+ proactive/reminder_context.rs::is_context_disabled async helper + is_context_disabled_in_prefs pure helper + PERSONA_DISABLE_KEYWORDS const 数组 + retrieve_for_due_reminders 内 disabled 短路；leftover refs 2 处 doc 字符串（time_ambiguity / reminder_context 注释）。**062 step6 全 4 子项完成**——自我进化细分收敛（006/017/019/026/035 全删；017 已在 step5g 顺手删）。**062 全 6 大 step 阶段全部完成**——可移动到 done）
- **062 收尾**：全部 step1-step6 完成，spec 内 30+ done req 已全部按删除清单执行。下次 cron 把 062 文档移到 docs/done/。
